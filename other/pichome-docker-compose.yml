version: "3"

services:
  pichome:
    platform: linux/amd64
    image: oaooa/pichome
    container_name: pichome
    ports:
      - 18080:80
    volumes:
      - "$HOME/docker/pichome/site:/var/www/html"